# build in function

# a function "min" returns the smmaller of two numbers. call "min" with two numbers as input
# print(min(22,45))

# a function "max" returns the larger of two numbers. call "max" with two numbers as input
# print(max(22,45))

# a function "sum" returns the sum of a list of numbers. call "sum" with a list of numbers as input
# print(sum([10,12,10]))

# a function "all" returns whether all booleans in a list are true. call "all" with a list of booleans as input
# print(all([True, False, True]))

# a function "any" returns whether any booleans in a list are true. call "any" with a list of booleans as input
# print(any([True, False, False]))

# a string method "lower" takes 0 arguments and returns a lowercase version of the string. lowercase "HAHAHA"
# print("HAHAHA".lower())

# a string method "lstrip" takes 0 arguments and removes leading spaces. Remove all leading spaces from " hehe"
# print(" hehe".lstrip())

# a string method "endswith" takes 1 string argument and returns True if the string ends with that argument. Check if "he@gmail.com" ends with ".com"
# print("he@gmail.com".endswith(".com"))
 
# a string method "startswith" takes 1 string argument and returs True if the strings starts with that argument. check if "Mr. Bean" starts with "Mr."
# print("Mr. Bean".startswith("Mr."))

# a string method "find" takes 1 string argument, and looks for that argument in the original string. find the position of "cali" in "supercalifraglilistic"
# print("supercalifragllistic".find("cali"))

# a string method "count" takes 1 string argument and counts the number of times afrument appears. find the number of "i"s in "mississippi"
# print("mississippi".count("i"))

# a string method "replace" takes two arguments, replacing all instances of the first string with the second string. Replace all "a"s in "hahahaha" with "e"s
# print("hahahaha".replace("a", "e"))

# replace all "Bob"s with "John" in "Bob poked Bob's dog"
# print("Bob poked Bob's dog".replace("Bob", "John"))

# a dictionary method "values" returns all values in the dictionary. Return all values in the dictionary {"a":1, "b":2}
# print({"a":1, "b":2}.values())

# a dictionary method "keys" returns all values in the dictionary. Return all keys in the dictionary {"a":1, "b":2}
# print({"a":1, "b":2}.keys())

# say you have a string ",". The string method "join" accepts a list of strings as input. For example , ",".join(["a","b","c"]) produces "a,b,c". Use "join" to convert a list of URL pieces into a full URL. eg: from ["google.com","mail","hi"] to "google.com/mail/hi". 
# print("/".join(["google.com","mail","hi"]))
